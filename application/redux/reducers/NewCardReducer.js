
const defaultState = {}

const newCard = (state = defaultState, { type, payload, ...action }) => {
    switch (type) {
        default:
            return { ...state }
    }
}

export default newCard